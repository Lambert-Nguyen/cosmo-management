# Aristay Environment Configuration Example
# Copy this file to .env and modify the values as needed
# Set DJANGO_ENVIRONMENT to control which settings are loaded
# Options: development, testing, production

# ---- Environment Selection ----
DJANGO_ENVIRONMENT=development

# ---- Django core ----
SECRET_KEY=your-secret-key-here
DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1,testserver

# ---- CORS / Frontend ----
CORS_ALLOW_ALL_ORIGINS=true
CORS_ALLOWED_ORIGINS=http://localhost:3000
FRONTEND_URL=http://localhost:3000

# ---- Email (real sending) ----
# EMAIL_HOST=smtp.mailersend.net
# EMAIL_PORT=587
# EMAIL_USE_TLS=true
# EMAIL_HOST_USER=your-email@example.com
# EMAIL_HOST_PASSWORD=your-app-password
# DEFAULT_FROM_EMAIL=noreply@aristay-internal.cloud

# ---- Email (dev sending) ----
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=localhost
EMAIL_PORT=1025
EMAIL_USE_TLS=false
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=

# ---- File storage & Cloudinary ----
USE_CLOUDINARY=true
CLOUDINARY_URL=cloudinary://your-cloudinary-url

# ---- Observability / infra (optional) ----
SENTRY_DSN=
REDIS_URL=
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/aristay_local
FCM_SERVER_KEY=
